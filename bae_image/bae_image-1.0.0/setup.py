# -*- coding: utf-8 -*-

from setuptools import setup, find_packages

setup(
    name = "bae_image",
    version = "1.0.0",
    packages = find_packages(),
    install_requires = ['bae_utils'],
    author = "bae",
    description = 'bae image sdk',
)
